{
    "scripts": {
      "test": "pip install -r requirements.txt && pytest"
    },
    "environments": {
      "test": {
        "pip": [],
        "addons": [
          { "plan": "basic", "app": "heroku-pip" }
        ]
      }
    }
  }